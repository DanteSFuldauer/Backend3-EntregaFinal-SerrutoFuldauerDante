paths:
  /api/adoptions:
    get:
      summary: Obtener todas las adopciones
      description: Get all adoptions
      tags:
        - Adoptions
      responses:
        200:
          description: Retorno de adopciones OK
          application/json:
            schema:
              type: array
              